apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: myapp
    
    "app-id": "myapp-123"
  name: myapp

spec:
  replicas: 1
  selector:
    matchLabels:
      app: myapp
  strategy: {}
  template:
    metadata:
      labels:
        app: myapp

        "app-id": "myapp-123"
    spec:
      containers:
      - image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        name: myapp
        args:
        - /bin/sh
        - -c
        - tail -f /dev/null
