apiVersion: v1
kind: Secret
metadata:
  # Note how the Secret is named
  labels:
    au: "0223092"
  name: {{.Values.name}}
  namespace: {{.Values.namespace}}
type: opaque
stringData:
  k2: "green"
